spring:
  application:
    name: api-generator
  datasource:
    # ?????
    driver-class-name: com.mysql.cj.jdbc.Driver
    # ?? api_generator_db ??????????????
    url: jdbc:mysql://localhost:3306/api_generator_db?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: ${DB_PASSWORD:123456}

mybatis-plus:
  configuration:
    # ????? SQL ?????? Phase 2 ??????
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    # ?????????? (???? core_entities ????? coreEntities)
    map-underscore-to-camel-case: true

# AI ????? (?????????????????????????)
ai:
  agent-a:
    api-key: ${AGENT_A_KEY:your_local_key_here}
  agent-b:
    api-key: ${AGENT_B_KEY:your_local_key_here}